# MCP Serve Hot Cache Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** `ccg serve` 세션 내에서 반복 MCP 툴 호출을 인메모리 TTL 캐시로 처리하여 DB 쿼리를 줄인다.

**Architecture:** `internal/mcp/cache.go`에 TTL 기반 `Cache` struct를 신규 구현하고, `handlers` struct에 `cache *Cache` 필드를 추가하여 15개 read-only 툴에 Get/Set 패턴을 적용한다. `build_or_update_graph`와 `run_postprocess` 완료 시 `cache.Flush()`로 전체 무효화한다. CLI `--cache-ttl` / `--no-cache` 플래그로 런타임 설정한다.

**Tech Stack:** Go, `sync.RWMutex`, `time.Duration`, Cobra, `encoding/json`

---

## 파일 구조

| 파일 | 변경 |
|------|------|
| `internal/mcp/cache.go` | 신규 — `Cache`, `entry`, `NewCache`, `Get`, `Set`, `Flush`, `cleanup` |
| `internal/mcp/cache_test.go` | 신규 — 단위 테스트 |
| `internal/mcp/server.go` | 수정 — `Deps`에 `Cache *Cache` 필드 추가, `handlers` 초기화 시 전달 |
| `internal/mcp/handlers.go` | 수정 — `handlers` struct에 `cache *Cache`, `mustJSON` 헬퍼, 15개 read-only 핸들러에 캐시 패턴, write 핸들러에 `Flush` |
| `internal/mcp/handlers_test.go` | 수정 — 캐시 히트/미스 검증 테스트 추가 |
| `internal/cli/serve.go` | 수정 — `ServeConfig`에 `CacheTTL`, `NoCache` 추가 및 플래그 등록 |
| `cmd/ccg/main.go` | 수정 — `runServe`에 cfg 전달, `Cache` 객체 생성 후 `mcpDeps.Cache`에 주입 |

---

### Task 1: Cache struct 구현

**Files:**
- Create: `internal/mcp/cache.go`
- Create: `internal/mcp/cache_test.go`

- [ ] **Step 1: 실패하는 테스트 작성**

`internal/mcp/cache_test.go`:

```go
package mcp

import (
	"fmt"
	"testing"
	"time"
)

func TestCache_GetMiss(t *testing.T) {
	c := NewCache(5 * time.Minute)
	_, ok := c.Get("nonexistent")
	if ok {
		t.Fatal("expected miss, got hit")
	}
}

func TestCache_SetGet(t *testing.T) {
	c := NewCache(5 * time.Minute)
	c.Set("key1", "value1")
	val, ok := c.Get("key1")
	if !ok {
		t.Fatal("expected hit, got miss")
	}
	if val != "value1" {
		t.Fatalf("expected %q, got %q", "value1", val)
	}
}

func TestCache_TTLExpiry(t *testing.T) {
	c := NewCache(50 * time.Millisecond)
	c.Set("key1", "value1")
	time.Sleep(100 * time.Millisecond)
	_, ok := c.Get("key1")
	if ok {
		t.Fatal("expected TTL expiry, but got hit")
	}
}

func TestCache_Flush(t *testing.T) {
	c := NewCache(5 * time.Minute)
	c.Set("k1", "v1")
	c.Set("k2", "v2")
	c.Flush()
	if _, ok := c.Get("k1"); ok {
		t.Fatal("expected miss after Flush")
	}
	if _, ok := c.Get("k2"); ok {
		t.Fatal("expected miss after Flush")
	}
}

func TestCache_Concurrent(t *testing.T) {
	c := NewCache(5 * time.Minute)
	done := make(chan struct{})
	for i := 0; i < 10; i++ {
		go func(i int) {
			key := fmt.Sprintf("key%d", i)
			c.Set(key, key)
			c.Get(key)
			done <- struct{}{}
		}(i)
	}
	for i := 0; i < 10; i++ {
		<-done
	}
}
```

- [ ] **Step 2: 테스트 실패 확인**

```bash
CGO_ENABLED=1 go test -tags fts5 ./internal/mcp/... -run TestCache -count=1
```

Expected: `FAIL` — `NewCache undefined`

- [ ] **Step 3: Cache 구현**

`internal/mcp/cache.go`:

```go
package mcp

import (
	"sync"
	"time"
)

type entry struct {
	value     string
	expiresAt time.Time
}

// Cache is a simple in-memory TTL cache safe for concurrent use.
// A nil Cache is valid and behaves as a no-op (all Gets miss, Sets are ignored).
type Cache struct {
	mu      sync.RWMutex
	entries map[string]entry
	ttl     time.Duration
}

// NewCache creates a Cache with the given TTL and starts a background cleanup goroutine.
func NewCache(ttl time.Duration) *Cache {
	c := &Cache{
		entries: make(map[string]entry),
		ttl:     ttl,
	}
	go c.cleanup()
	return c
}

// Get returns the cached value and true if the key exists and has not expired.
func (c *Cache) Get(key string) (string, bool) {
	c.mu.RLock()
	e, ok := c.entries[key]
	c.mu.RUnlock()
	if !ok || time.Now().After(e.expiresAt) {
		return "", false
	}
	return e.value, true
}

// Set stores the value under key with the cache's configured TTL.
func (c *Cache) Set(key string, value string) {
	c.mu.Lock()
	c.entries[key] = entry{value: value, expiresAt: time.Now().Add(c.ttl)}
	c.mu.Unlock()
}

// Flush removes all entries from the cache.
func (c *Cache) Flush() {
	c.mu.Lock()
	c.entries = make(map[string]entry)
	c.mu.Unlock()
}

// cleanup runs every 30s and removes expired entries to prevent unbounded growth.
func (c *Cache) cleanup() {
	ticker := time.NewTicker(30 * time.Second)
	defer ticker.Stop()
	for range ticker.C {
		now := time.Now()
		c.mu.Lock()
		for k, e := range c.entries {
			if now.After(e.expiresAt) {
				delete(c.entries, k)
			}
		}
		c.mu.Unlock()
	}
}
```

- [ ] **Step 4: 테스트 통과 확인**

```bash
CGO_ENABLED=1 go test -tags fts5 ./internal/mcp/... -run TestCache -race -count=1
```

Expected: `ok` (모든 5개 테스트 통과, race detector 무결)

- [ ] **Step 5: 커밋**

```bash
git add internal/mcp/cache.go internal/mcp/cache_test.go
git commit -m "feat: add in-memory TTL cache for MCP serve sessions"
```

---

### Task 2: Cache를 Deps와 handlers에 연결

**Files:**
- Modify: `internal/mcp/server.go`
- Modify: `internal/mcp/handlers.go` (struct 필드만 추가)

이 태스크는 구조적 변경(Structural Change)만 포함한다. 동작은 변경되지 않는다.

- [ ] **Step 1: `Deps`에 `Cache *Cache` 필드 추가**

`internal/mcp/server.go`의 `Deps` struct (현재 line 73):

```go
type Deps struct {
	Store            store.GraphStore
	DB               *gorm.DB
	Parser           Parser
	SearchBackend    storesearch.Backend
	ImpactAnalyzer   ImpactAnalyzer
	FlowTracer       FlowTracer
	ChangesGitClient changes.GitClient
	Logger           *slog.Logger

	// Phase 11 추가
	QueryService      QueryService
	LargefuncAnalyzer LargefuncAnalyzer
	DeadcodeAnalyzer  DeadcodeAnalyzer
	CouplingAnalyzer  CouplingAnalyzer
	CoverageAnalyzer  CoverageAnalyzer
	CommunityBuilder  CommunityBuilder
	Incremental       IncrementalSyncer

	// Cache — nil이면 캐시 비활성화
	Cache *Cache
}
```

- [ ] **Step 2: `handlers` struct에 `cache *Cache` 추가**

`internal/mcp/handlers.go`의 `handlers` struct (현재 line 24):

```go
type handlers struct {
	deps  *Deps
	cache *Cache
}
```

- [ ] **Step 3: `NewServer`에서 `handlers` 초기화 시 cache 전달**

`internal/mcp/server.go` line 106:

```go
h := &handlers{deps: deps, cache: deps.Cache}
```

- [ ] **Step 4: 빌드 확인**

```bash
CGO_ENABLED=1 go build -tags fts5 ./...
```

Expected: 빌드 성공, 에러 없음

- [ ] **Step 5: 기존 테스트 통과 확인**

```bash
CGO_ENABLED=1 go test -tags fts5 ./internal/mcp/... -count=1
```

Expected: `ok` (기존 테스트 모두 통과)

- [ ] **Step 6: 커밋**

```bash
git add internal/mcp/server.go internal/mcp/handlers.go
git commit -m "refactor: wire Cache field into Deps and handlers struct"
```

---

### Task 3: 15개 read-only 핸들러에 캐시 패턴 적용

**Files:**
- Modify: `internal/mcp/handlers.go`
- Modify: `internal/mcp/handlers_test.go`

**캐시 패턴 (모든 read-only 핸들러에 동일하게 적용):**

```go
// 1. 캐시 키 생성
key := "tool_name:" + mustJSON(map[string]any{"arg1": val1, "arg2": val2})

// 2. 캐시 조회
if h.cache != nil {
    if cached, ok := h.cache.Get(key); ok {
        return mcp.NewToolResultText(cached), nil
    }
}

// ... 기존 DB 쿼리 로직 ...

// 3. 결과 캐싱 (함수 return 직전)
result := string(b) // json.Marshal 결과
if h.cache != nil {
    h.cache.Set(key, result)
}
return mcp.NewToolResultText(result), nil
```

**`mustJSON` 헬퍼 추가** (`handlers.go` 파일 상단, `import` 블록 아래):

```go
// mustJSON serializes v to a JSON string for use as a cache key.
// Panics are not possible: json.Marshal only fails on unsupported types,
// and we only pass map[string]any with string/int values.
func mustJSON(v any) string {
	b, _ := json.Marshal(v)
	return string(b)
}
```

- [ ] **Step 1: 캐시 히트 테스트 작성**

`internal/mcp/handlers_test.go`에 추가 (기존 DB 셋업 helper 재사용):

```go
func TestGetNode_CacheHit(t *testing.T) {
	db := openTestDB(t)
	st := gormstore.New(db)
	if err := st.AutoMigrate(); err != nil {
		t.Fatal(err)
	}

	// 노드 삽입
	node := model.Node{
		QualifiedName: "pkg.CacheHitFunc",
		Name:          "CacheHitFunc",
		Kind:          model.NodeKindFunction,
		FilePath:      "pkg/cache_hit.go",
		Language:      "go",
	}
	if err := db.Create(&node).Error; err != nil {
		t.Fatal(err)
	}

	cache := NewCache(5 * time.Minute)
	h := &handlers{
		deps:  &Deps{Store: st, DB: db},
		cache: cache,
	}

	req := newCallToolRequest(t, "get_node", map[string]any{
		"qualified_name": "pkg.CacheHitFunc",
	})

	// 1차 호출: DB에서 가져와 캐시에 저장
	res1, err := h.getNode(context.Background(), req)
	if err != nil || res1.IsError {
		t.Fatalf("first call failed: %v, isError=%v", err, res1.IsError)
	}

	// DB에서 노드 삭제 (캐시 히트 검증용)
	if err := db.Delete(&model.Node{}, node.ID).Error; err != nil {
		t.Fatal(err)
	}

	// 2차 호출: 캐시에서 응답 (DB에 없어도 성공해야 함)
	res2, err := h.getNode(context.Background(), req)
	if err != nil || res2.IsError {
		t.Fatalf("second call (cache hit) failed: isError=%v", res2.IsError)
	}
}

func TestGetNode_NoCache(t *testing.T) {
	db := openTestDB(t)
	st := gormstore.New(db)
	if err := st.AutoMigrate(); err != nil {
		t.Fatal(err)
	}

	h := &handlers{
		deps:  &Deps{Store: st, DB: db},
		cache: nil, // 캐시 없음
	}

	req := newCallToolRequest(t, "get_node", map[string]any{
		"qualified_name": "pkg.NotExist",
	})

	res, err := h.getNode(context.Background(), req)
	if err != nil {
		t.Fatal(err)
	}
	// 캐시 없이 DB 조회 → not found error 응답
	if !res.IsError {
		t.Fatal("expected error response for missing node without cache")
	}
}
```

`handlers_test.go`에 이미 있는 `openTestDB(t)` helper를 확인 후 없으면 아래를 추가:

```go
func openTestDB(t *testing.T) *gorm.DB {
	t.Helper()
	db, err := gorm.Open(sqlite.Open(":memory:"), &gorm.Config{Logger: logger.Discard})
	if err != nil {
		t.Fatalf("open test db: %v", err)
	}
	return db
}
```

`handlers_test.go`에 이미 있는 `newCallToolRequest` helper를 확인 후 없으면 아래를 추가:

```go
func newCallToolRequest(t *testing.T, tool string, args map[string]any) mcp.CallToolRequest {
	t.Helper()
	b, _ := json.Marshal(args)
	var req mcp.CallToolRequest
	req.Params.Name = tool
	req.Params.Arguments = args
	_ = b
	return req
}
```

- [ ] **Step 2: 테스트 실패 확인**

```bash
CGO_ENABLED=1 go test -tags fts5 ./internal/mcp/... -run "TestGetNode_Cache" -count=1
```

Expected: `FAIL` — cache miss on second call (DB 삭제 후 노드를 못 찾음)

- [ ] **Step 3: `mustJSON` 헬퍼 추가**

`internal/mcp/handlers.go`의 `package mcp` 선언 직후, `import` 블록 아래에 추가:

```go
// mustJSON serializes v to a JSON string for use as a cache key.
func mustJSON(v any) string {
	b, _ := json.Marshal(v)
	return string(b)
}
```

- [ ] **Step 4: 15개 read-only 핸들러에 캐시 패턴 적용**

아래 목록에 따라 각 핸들러의 (a) 함수 시작부 → 캐시 Get, (b) `return mcp.NewToolResultText(string(b))` 직전 → 캐시 Set 패턴을 적용한다.

**캐시 키 스키마 (핸들러 이름: 키 형식):**

| 핸들러 | 캐시 키 |
|--------|---------|
| `getNode` | `"get_node:" + mustJSON(map[string]any{"qualified_name": qn})` |
| `getImpactRadius` | `"get_impact_radius:" + mustJSON(map[string]any{"qualified_name": qn, "depth": depth})` |
| `search` | `"search:" + mustJSON(map[string]any{"query": query, "limit": limit, "path": pathPrefix})` |
| `getAnnotation` | `"get_annotation:" + mustJSON(map[string]any{"qualified_name": qn})` |
| `traceFlow` | `"trace_flow:" + mustJSON(map[string]any{"qualified_name": qn})` |
| `queryGraph` | `"query_graph:" + mustJSON(map[string]any{"pattern": pattern, "target": target})` |
| `listGraphStats` | `"list_graph_stats:{}"` (상수 키) |
| `findLargeFunctions` | `"find_large_functions:" + mustJSON(map[string]any{"min_lines": minLines, "limit": limit, "path": pathPrefix})` |
| `detectChanges` | `"detect_changes:" + mustJSON(map[string]any{"repo_root": repoRoot, "base": base})` |
| `getAffectedFlows` | `"get_affected_flows:" + mustJSON(map[string]any{"repo_root": repoRoot, "base": base})` |
| `listFlows` | `"list_flows:" + mustJSON(map[string]any{"sort_by": sortBy, "limit": limit})` |
| `listCommunities` | `"list_communities:" + mustJSON(map[string]any{"sort_by": sortBy, "min_size": minSize})` |
| `getCommunity` | `"get_community:" + mustJSON(map[string]any{"community_id": communityID, "include_members": includeMembers})` |
| `getArchitectureOverview` | `"get_architecture_overview:{}"` (상수 키) |
| `findDeadCode` | `"find_dead_code:" + mustJSON(map[string]any{"path": pathPrefix})` |

**`getNode` 핸들러 적용 예시** (전체 코드):

```go
func (h *handlers) getNode(ctx context.Context, request mcp.CallToolRequest) (*mcp.CallToolResult, error) {
	log := h.logger()

	qn, err := request.RequireString("qualified_name")
	if err != nil {
		return mcp.NewToolResultError(fmt.Sprintf("missing parameter: %v", err)), nil
	}

	log.Info("get_node called", "qualified_name", qn)

	key := "get_node:" + mustJSON(map[string]any{"qualified_name": qn})
	if h.cache != nil {
		if cached, ok := h.cache.Get(key); ok {
			return mcp.NewToolResultText(cached), nil
		}
	}

	node, err := h.deps.Store.GetNode(ctx, qn)
	if err != nil {
		log.Error("store error", "tool", "get_node", "error", err)
		return mcp.NewToolResultError(fmt.Sprintf("store error: %v", err)), nil
	}
	if node == nil {
		log.Warn("node not found", "qualified_name", qn)
		return mcp.NewToolResultError(fmt.Sprintf("node %q not found", qn)), nil
	}

	data := map[string]any{
		"id":             node.ID,
		"qualified_name": node.QualifiedName,
		"kind":           node.Kind,
		"name":           node.Name,
		"file_path":      node.FilePath,
		"start_line":     node.StartLine,
		"end_line":       node.EndLine,
		"language":       node.Language,
	}
	b, _ := json.Marshal(data)
	result := string(b)
	if h.cache != nil {
		h.cache.Set(key, result)
	}
	return mcp.NewToolResultText(result), nil
}
```

위 패턴을 나머지 14개 핸들러에 동일하게 적용한다. 각 핸들러에서:
1. 파라미터 파싱 직후 → 캐시 키 생성 + `h.cache.Get(key)` 체크
2. 마지막 `return mcp.NewToolResultText(string(b)), nil` → `result := string(b)` + `h.cache.Set(key, result)` + `return mcp.NewToolResultText(result), nil`

에러 경로 (`return mcp.NewToolResultError(...)`)에는 캐시 Set 하지 않는다.

- [ ] **Step 5: 테스트 통과 확인**

```bash
CGO_ENABLED=1 go test -tags fts5 ./internal/mcp/... -run "TestGetNode_Cache" -count=1
```

Expected: `ok`

- [ ] **Step 6: 전체 MCP 테스트 통과 확인**

```bash
CGO_ENABLED=1 go test -tags fts5 ./internal/mcp/... -count=1
```

Expected: `ok`

- [ ] **Step 7: 커밋**

```bash
git add internal/mcp/handlers.go internal/mcp/handlers_test.go
git commit -m "feat: apply TTL cache to 15 read-only MCP handlers"
```

---

### Task 4: write 핸들러 완료 시 자동 Flush

**Files:**
- Modify: `internal/mcp/handlers.go`
- Modify: `internal/mcp/handlers_test.go`

- [ ] **Step 1: Flush 테스트 작성**

`internal/mcp/handlers_test.go`에 추가:

```go
func TestBuildOrUpdateGraph_FlushesCache(t *testing.T) {
	db := openTestDB(t)
	st := gormstore.New(db)
	if err := st.AutoMigrate(); err != nil {
		t.Fatal(err)
	}

	cache := NewCache(5 * time.Minute)
	cache.Set("get_node:{\"qualified_name\":\"pkg.Foo\"}", `{"id":1}`)

	// build_or_update_graph 호출 후 캐시가 비워져야 한다
	h := &handlers{
		deps: &Deps{
			Store:  st,
			DB:     db,
			Parser: &simpleGoParser{},
		},
		cache: cache,
	}

	req := newCallToolRequest(t, "build_or_update_graph", map[string]any{
		"path":         t.TempDir(), // 빈 디렉토리
		"full_rebuild": true,
		"postprocess":  "none",
	})

	_, err := h.buildOrUpdateGraph(context.Background(), req)
	if err != nil {
		t.Fatal(err)
	}

	// 캐시가 flush되었는지 확인
	if _, ok := cache.Get("get_node:{\"qualified_name\":\"pkg.Foo\"}"); ok {
		t.Fatal("expected cache to be flushed after buildOrUpdateGraph")
	}
}
```

- [ ] **Step 2: 테스트 실패 확인**

```bash
CGO_ENABLED=1 go test -tags fts5 ./internal/mcp/... -run TestBuildOrUpdateGraph_FlushesCache -count=1
```

Expected: `FAIL` — 캐시 엔트리가 Flush되지 않음

- [ ] **Step 3: `buildOrUpdateGraph`에 `Flush` 추가**

`internal/mcp/handlers.go`의 `buildOrUpdateGraph` 함수 끝부분 (현재 `return mcp.NewToolResultText(string(b)), nil` 직전):

```go
	elapsed := time.Since(start).Milliseconds()

	result := map[string]any{
		"status":        "ok",
		"files_parsed":  fileCount,
		"nodes_created": nodeCount,
		"edges_created": edgeCount,
		"elapsed_ms":    elapsed,
	}
	b, _ := json.Marshal(result)

	if h.cache != nil {
		h.cache.Flush()
	}
	return mcp.NewToolResultText(string(b)), nil
```

- [ ] **Step 4: `runPostprocess`에 `Flush` 추가**

`internal/mcp/handlers.go`의 `runPostprocess` 함수 끝부분:

```go
	result := map[string]any{
		"status":            "ok",
		"flows_count":       flowsCount,
		"communities_count": communitiesCount,
		"fts_indexed":       ftsIndexed,
	}
	b, _ := json.Marshal(result)

	if h.cache != nil {
		h.cache.Flush()
	}
	return mcp.NewToolResultText(string(b)), nil
```

- [ ] **Step 5: 테스트 통과 확인**

```bash
CGO_ENABLED=1 go test -tags fts5 ./internal/mcp/... -run TestBuildOrUpdateGraph_FlushesCache -count=1
```

Expected: `ok`

- [ ] **Step 6: 전체 테스트 통과 확인**

```bash
CGO_ENABLED=1 go test -tags fts5 ./internal/mcp/... -count=1
```

Expected: `ok`

- [ ] **Step 7: 커밋**

```bash
git add internal/mcp/handlers.go internal/mcp/handlers_test.go
git commit -m "feat: flush MCP cache on build_or_update_graph and run_postprocess"
```

---

### Task 5: CLI 플래그 및 main.go 연결

**Files:**
- Modify: `internal/cli/serve.go`
- Modify: `cmd/ccg/main.go`

- [ ] **Step 1: `ServeConfig` 수정 및 플래그 등록**

`internal/cli/serve.go` 전체:

```go
package cli

import (
	"time"

	"github.com/spf13/cobra"
)

// ServeConfig holds parsed flags for the serve subcommand.
type ServeConfig struct {
	CacheTTL time.Duration
	NoCache  bool
}

func newServeCmd(deps *Deps) *cobra.Command {
	var cfg ServeConfig

	cmd := &cobra.Command{
		Use:   "serve",
		Short: "Start the MCP server over stdio",
		Args:  cobra.NoArgs,
		RunE: func(cmd *cobra.Command, args []string) error {
			if deps.ServeFunc != nil {
				return deps.ServeFunc(cfg)
			}
			return fmt.Errorf("ServeFunc not configured")
		},
	}

	cmd.Flags().DurationVar(&cfg.CacheTTL, "cache-ttl", 5*time.Minute, "TTL for MCP serve session cache (0 or --no-cache to disable)")
	cmd.Flags().BoolVar(&cfg.NoCache, "no-cache", false, "Disable in-memory cache for MCP serve session")

	return cmd
}
```

`fmt` import 추가 필요:

```go
import (
	"fmt"
	"time"

	"github.com/spf13/cobra"
)
```

- [ ] **Step 2: `runServe`에 cfg 전달 및 Cache 생성**

`cmd/ccg/main.go`에서 `deps.ServeFunc` 클로저 수정:

```go
deps.ServeFunc = func(cfg cli.ServeConfig) error {
    return runServe(deps, cfg)
}
```

`runServe` 함수 시그니처 변경 및 Cache 생성 추가:

```go
func runServe(deps *cli.Deps, cfg cli.ServeConfig) error {
	deps.Logger.Info("starting code-context-graph MCP server")

	walker := treesitter.NewWalker(treesitter.GoSpec, treesitter.WithLogger(deps.Logger))

	var cache *mcpserver.Cache
	if !cfg.NoCache && cfg.CacheTTL > 0 {
		cache = mcpserver.NewCache(cfg.CacheTTL)
		deps.Logger.Info("MCP cache enabled", "ttl", cfg.CacheTTL)
	}

	mcpDeps := &mcpserver.Deps{
		Store:             deps.Store,
		DB:                deps.DB,
		Parser:            walker,
		SearchBackend:     deps.SearchBackend,
		ImpactAnalyzer:    impact.New(deps.Store),
		FlowTracer:        flows.New(deps.Store),
		ChangesGitClient:  changes.NewExecGitClient(),
		QueryService:      query.New(deps.DB),
		LargefuncAnalyzer: largefunc.New(deps.DB),
		DeadcodeAnalyzer:  deadcode.New(deps.DB),
		CouplingAnalyzer:  coupling.New(deps.DB),
		CoverageAnalyzer:  coverage.New(deps.DB),
		CommunityBuilder:  community.New(deps.DB),
		Logger:            deps.Logger,
		Cache:             cache,
	}

	srv := mcpserver.NewServer(mcpDeps)

	deps.Logger.Info("serving MCP over stdio")
	if err := server.ServeStdio(srv); err != nil {
		return fmt.Errorf("MCP server: %w", err)
	}
	return nil
}
```

- [ ] **Step 3: 빌드 확인**

```bash
CGO_ENABLED=1 go build -tags fts5 ./...
```

Expected: 빌드 성공

- [ ] **Step 4: 전체 테스트 통과 확인**

```bash
CGO_ENABLED=1 go test -tags fts5 ./... -count=1
```

Expected: `ok` (전체 패키지 통과)

- [ ] **Step 5: 통합 확인 — `ccg serve --help`**

```bash
go run -tags fts5 ./cmd/ccg serve --help
```

Expected: 출력에 `--cache-ttl` 과 `--no-cache` 플래그가 표시됨:
```
Flags:
      --cache-ttl duration   TTL for MCP serve session cache (0 or --no-cache to disable) (default 5m0s)
      --no-cache             Disable in-memory cache for MCP serve session
```

- [ ] **Step 6: 커밋**

```bash
git add internal/cli/serve.go cmd/ccg/main.go
git commit -m "feat: add --cache-ttl and --no-cache flags to ccg serve"
```
